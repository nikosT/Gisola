INTERFACE

      SUBROUTINE APPLY(DATA, NSAMPS, ZP, SN, SD, NSECTS, MAX_NT)
      IMPLICIT NONE
!$OMP DECLARE TARGET
!$ACC ROUTINE SEQ
      INTEGER MAX_NT, NSAMPS, NSECTS
      REAL*4 SN(*), SD(*), DATA(MAX_NT)
      LOGICAL ZP

      END SUBROUTINE APPLY

END INTERFACE

